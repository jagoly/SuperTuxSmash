
//========================================================//

foreign class World {

  foreign random_int(min, max)
  foreign random_float(min, max)

  foreign cancel_sound(id)
  foreign cancel_effect(id)
}
